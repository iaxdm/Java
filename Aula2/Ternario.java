public class Ternario {
    public static void main(String[] args) {
        int QntDiaMes = 28;
        System.out.println(QntDiaMes < 30 ? "Mês é fevereiro" : "Mês não é fevereiro");
        // Outra forma de escrever o IF e ELSE ? representa o IF : representa o else
    }
}
